# .github/workflows/playwright.yml
# CI/CD pour les tests Playwright
# ─────────────────────────────────────────────────────────────

name: Playwright Tests

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    # Services (décommenter si base de données de test nécessaire)
    # services:
    #   postgres:
    #     image: postgres:15
    #     env:
    #       POSTGRES_PASSWORD: postgres
    #     ports:
    #       - 5432:5432

    env:
      # Variables d'environnement pour les tests
      # ⚠️ Utiliser 127.0.0.1 (pas localhost) pour éviter ECONNREFUSED ::1 sur Ubuntu
      # Node.js DNS résout localhost → ::1 (IPv6) mais Next.js écoute sur IPv4 uniquement
      NEXT_PUBLIC_APP_URL: http://127.0.0.1:3000
      PLAYWRIGHT_BASE_URL: http://127.0.0.1:3000
      # Supabase test credentials (noms des secrets GitHub)
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.TEST_SUPABASE_URL }}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.TEST_SUPABASE_ANON_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.TEST_SERVICE_ROLE_KEY }}
      # Clé API IA pour les tests
      ZHIPU_API_KEY: ${{ secrets.GEMINI_API_KEY }}
      INTERNAL_API_SECRET: test-secret-internal

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🎭 Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: 🔨 Build application
        run: npm run build
        env:
          NEXT_TELEMETRY_DISABLED: 1

      - name: 🧪 Run Playwright tests
        run: npx playwright test --reporter=html

      - name: 📊 Upload Playwright report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: 📸 Upload test screenshots
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-screenshots
          path: test-results/
          retention-days: 7

  # Job optionnel pour les tests de régression visuelle
  visual-regression:
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'pull_request'
    
    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🎭 Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: 📸 Visual regression tests
        run: npx playwright test --project=chromium --grep "@visual"
        continue-on-error: true

  # Notification Slack/Discord (optionnel)
  notify:
    runs-on: ubuntu-latest
    needs: [test]
    if: failure() && github.event_name == 'push'
    
    steps:
      - name: 📢 Notify on failure
        run: |
          echo "Tests failed on ${{ github.ref }}"
          # Ajouter notification Slack/Discord ici si nécessaire